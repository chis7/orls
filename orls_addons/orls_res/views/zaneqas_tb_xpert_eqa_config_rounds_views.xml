<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="orls_medical_disciplines_tree" model="ir.ui.view">
        <field name="name">orls_medical_disciplines_tree</field>
        <field name="model">orls.medical.disciplines</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="orls_medical_disciplines_form" model="ir.ui.view">
        <field name="name">orls_medical_disciplines_form</field>
        <field name="model">orls.medical.disciplines</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_save_eqa_config_round_as_draft" data-hotkey="v"
                            confirm="Are you sure you want to save as draft?"
                            string="Save as Draft" class="btn-primary" type="object"
                            context="{'validate_analytic': True}"
                            invisible="state != 'draft' "/>
                    <button name="action_submit_eqa_config_round_to_supervisor" data-hotkey="v"
                            confirm="Are you sure you want to make this submission?"
                            string="Save and Submit" class="btn-primary" type="object"
                            context="{'validate_analytic': True}"
                            invisible="state != 'draft' "/>
                    <button name="action_supervisor_approve_eqa_config_round" data-hotkey="v"
                            confirm="Are you sure you want to make this approval?"
                            string="Approve" class="btn-primary" type="object" context="{'validate_analytic': True}"
                            invisible="state != 'supervisor' "/>
                    <button name="action_supervisor_send_back_eqa_config_round" data-hotkey="v"
                            confirm="Are you sure you want to send back?"
                            string="Send Back" class="btn-primary" type="object" context="{'validate_analytic': True}"
                            invisible="state != 'supervisor' "/>


                    <field name="state" context="{'validate_analytic': True}" widget="statusbar"
                           statusbar_visible="draft,supervisor,approved"
                           options="{'clickable':'1'}"/>

                </header>

                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                        </group>
                    </group>


                </sheet>
                <div class="oe_chatter" context="{'validate_analytic': True}">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="orls_medical_disciplines_tree_search" model="ir.ui.view">
        <field name="name">orls_medical_disciplines_search</field>
        <field name="model">orls.medical.disciplines</field>
        <field name="arch" type="xml">
            <search string="Medical Disciplines">
                <separator/>
            </search>
        </field>
    </record>

    <record id="action_orls_medical_disciplines" model="ir.actions.act_window">
        <field name="name">Medical Disciplines</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">orls.medical.disciplines</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                create a medical discipline
            </p>
        </field>
    </record>

</odoo>
